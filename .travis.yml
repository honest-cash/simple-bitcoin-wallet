notifications:
  slack: vqlabs:j71yKGBW4ipmLoWcJuteN83i

cache:
  directories:
    - node_modules

addons:
  sauce_connect: true

matrix:
  include:
  - name: "simple-bitcoin-wallet"
    sudo: false
    os: osx
    osx_image: xcode10.1
    before_script:
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start

    install:
    - npm install

    script:
    - npm run build
    - npm run test
